<div class="container">
  <div class="row mx-auto">
    <h2 class="col-12">パスワードをお忘れですか?</h2>
  </div>

  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>
    <div class="form-horizontal mt-4">
      <div class="form-group">
        <%= f.label :メールアドレス, class: 'col-2 offset' %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control-sm col-4 ml-4' %>
      </div>

      <div class="actions col offset">
        <%= f.submit "空メールを送信する", class: 'btn btn-primary col-2 ml-4 mb-4' %>
      </div>
    </div>
  <% end %>

  <!--<%= render "devise/shared/links" %>-->

  <div class="row mb-4">
    <h2 class="col-12 offset-1">既に登録済みの方</h2>
    <%= link_to 'こちら', user_session_path, class: 'col-1 offset-2' %> からログインしてください。
    <h4 class="col-12 offset-1 mt-5">会員登録がお済みでない方</h4>
    <%= link_to 'こちら', new_user_registration_path, class: 'col-1 offset-2' %>から新規登録を行ってください。
  </div>
